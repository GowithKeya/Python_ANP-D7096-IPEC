# Python Training

This repository contains beginner-friendly Python practice programs based on real-life problem statements. The examples are designed to help learners understand arithmetic operators, user input, variables, and basic output formatting.

## Topics Covered

- Addition using `+`
- Subtraction using `-`
- Multiplication using `*`
- Division using `/`
- Floor division using `//`
- Modulus using `%`
- Exponentiation using `**`
- Taking input with `input()`
- Converting values using `int()` and `float()`
- Printing results with `print()`

## Practice Programs

| No. | File | Problem |
| --- | --- | --- |
| 1 | `01_monthly_salary_calculation.py` | Calculate total salary using basic salary and incentive |
| 2 | `02_grocery_store_bill.py` | Calculate total bill for rice packets |
| 3 | `03_fuel_consumption_tracker.py` | Calculate car mileage |
| 4 | `04_bank_account_balance.py` | Calculate remaining bank balance after withdrawal |
| 5 | `05_classroom_seating_arrangement.py` | Find complete classroom rows |
| 6 | `06_pizza_distribution.py` | Find remaining pizza slices after distribution |
| 7 | `07_compound_growth_of_savings.py` | Calculate savings growth when amount doubles every year |
| 8 | `08_online_shopping_discount.py` | Calculate final price after discount |
| 9 | `09_mobile_recharge_plan.py` | Calculate total mobile recharge cost |
| 10 | `10_event_management_budget.py` | Calculate amount per event participant |
| 11 | `11_food_delivery_business_profit_analysis.py` | Analyze revenue, profit, boxes, remaining packets, and future revenue |

Each Python file includes the question as comments at the top, followed by the solution code.

## How to Run

Make sure Python is installed, then open a terminal in this folder.

Run any program using:

```bash
python file_name.py
```

Example:

```bash
python 01_monthly_salary_calculation.py
```

Then enter the requested input values and check the output.

## Example

```text
Enter basic salary: 25000
Enter incentive amount: 5000
Total Salary: 30000.0
```

## Goal

The goal of this training project is to build confidence with simple Python programs before moving to conditions, loops, functions, lists, and larger projects.
